# CI 검증용 제출물 (#307)

> 분류: 운영
> 작성: 오흥재 · 2026-09-08 17:05
> 근거: 실측 (이슈 #307 · 옛 커밋 a227c9f 에서 딴 브랜치로 관문 확인)
> 요지: 2026-08-25 커밋에서 딴 브랜치의 PR 이 초록불이 되는지 실제로 본다. 확인 뒤 닫는다.
> 상태: 초안

## 무엇을 보는가

이 브랜치는 `a227c9f` (2026-08-25) 에서 땄다. 그 시점에는

- `inbox-pr-alert.yml` 이 `on: pull_request` 였다
- `notify_send.sh` 가 없었다

**그런데도 초록불이면** 다음이 증명된다.

1. PR 체크는 head 가 아니라 base(main) 의 워크플로로 돈다
2. 브랜치에 main 을 손으로 넣지 않아도 최신 판이 적용된다
3. 알림 워크플로는 이제 PR 을 막지 못한다

확인 뒤 이 PR 은 닫고 브랜치도 지운다.
