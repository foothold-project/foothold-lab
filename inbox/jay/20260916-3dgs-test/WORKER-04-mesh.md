# 작업서 04 · 스플랫 → 2.5D 지면 메시 (경로 A) 와 지표

> 분류: 계획
> 작성: 오흥재 · 2026-09-16 14:50
> 근거: RESEARCH-3dgs-terrain.md G4-2 · G4-3 · G5-2 · 작업서 02 · 03 산출물
> 요지: 스플랫 중심점을 바닥 평면에 정렬해 미터 단위 높이 격자로 만들고, 관측 비율 · 평면 잔차 · 삼각형 수를 함께 남겨 «매끄럽게 메시화됐는가» 를 수치로 답한다
> 상태: 확정
> 판: v1.0
> 이슈: #430

워커 모델: `gpt-5.6-sol` · reasoning high (수치 알고리즘 구현 · 좌표계 처리)

선행 조건: `_out/splat/splat.ply` (작업서 03) 와 `_out/colmap/sparse/0_txt/` (작업서 02 · cameras.txt · images.txt) 가 있어야 한다. 없으면 «선행 조건 없음» 으로 보고하고 멈춘다.

## 0. 자리와 경로

| 무엇 | 절대 경로 |
|---|---|
| 작업 폴더 | `C:/Users/AI-WS01/Desktop/jay/인공지능사관학교/foothold-lab/inbox/jay/20260916-3dgs-test/` |
| 스크립트 (새로 만든다 · 저장소에 들어간다) | 작업 폴더 `poc_mesh_grid.py` |
| 입력 | `_out/splat/splat.ply` · `_out/colmap/sparse/0_txt/images.txt` · `cameras.txt` |
| 산출물 | `_out/mesh/ground.obj` · `_out/mesh/ground_heightmap.png` · `_out/mesh/ground_stats.json` · `_out/mesh/REPORT-04.md` |
| 파이썬 | `C:/Users/AI-WS01/anaconda3/envs/isaac311/python.exe` (numpy · scipy · trimesh · OpenCV 있음. **패키지를 새로 설치하지 않는다**) |

한글 경로다. 파일 읽기 · 쓰기는 `open()` 과 `numpy` 로 하고, `cv2.imread`/`imwrite` 는 유니코드 경로에 약하니 `cv2.imencode` + `tofile` 을 쓴다.

## 1. 알고리즘 (이 순서 · 매개변수는 인자로 · 기본값 명시)

1. **PLY 읽기.** 3DGS PLY 는 binary_little_endian 이고 속성이 `x y z nx ny nz f_dc_0..2 f_rest_* opacity scale_0..2 rot_0..3` 이다. 헤더를 파싱해 numpy 구조체로 읽는 작은 리더를 스크립트 안에 쓴다 (plyfile 설치 금지). 읽은 개수와 속성 목록을 보고서에 적는다.
2. **거르기.** 불투명도 `sigmoid(opacity) >= 0.5` (인자 `--min_opacity`), 크기 `max(exp(scale)) <= 0.5 · 장면 단위` (인자 `--max_scale` · 축척 뒤 미터로 0.3 m 이하로 다시 적용). 거른 전후 개수를 적는다.
3. **카메라.** `images.txt` 의 쿼터니언 · 이동에서 카메라 중심 `C = -R^T t` 와 카메라 «아래» 방향(COLMAP 카메라 좌표의 +y 를 월드로 돌린 벡터)을 구한다. 중심 궤적과 평균 아래 방향을 적는다.
4. **바닥 평면.** 카메라 궤적에서 반지름 4 (장면 단위 · 인자) 안의 점 가운데 아래 방향 쪽에 있는 점을 후보로 RANSAC 평면 적합(반복 2,000 · 거리 문턱은 점 간 중앙 거리의 2 배). 평면 법선이 평균 아래 방향과 반대(위를 향하게) 가 되도록 부호를 맞춘다. 인라이어 수 · 잔차 RMS 를 적는다.
5. **축척(추측).** 카메라 중심에서 평면까지의 수직 거리 중앙값이 `--camera_height_m` (기본 1.4 m · 손에 든 폰 높이 가정 · `추측`) 이 되도록 배율 s 를 정한다. 배율 · 중앙값 · 사분위를 적는다. 이 값은 추측이므로 보고서에 그렇게 표기한다.
6. **좌표 변환.** 평면 법선 → +Z, 평면 위 → z=0, 카메라 궤적의 xy 중심 → 원점, 궤적의 주 진행 방향 → +X 가 되도록 회전 · 이동 · 배율을 한 번에 적용한 4x4 행렬을 만들고 `ground_stats.json` 에 저장한다 (뒤 단계에서 스플랫 렌더와 좌표를 맞출 때 쓴다).
7. **높이 격자.** 셀 0.05 m (인자 `--cell`), 범위는 궤적 xy 를 감싸는 상자에 여유 3 m (인자). z 가 [-0.5, +1.0] m 인 점만 지면 후보. 셀마다 점이 3 개 이상이면 z 의 중앙값, 아니면 «관측 없음». **관측 셀 비율**을 전체와 «궤적에서 1.5 m 이내(보행 구간)» 로 나눠 적는다.
8. **구멍 메움.** 관측 없는 셀은 가장 가까운 관측 셀의 값을 거리 가중 평균으로 채운다(scipy `distance_transform_edt` 로 최근접 인덱스). 채운 셀은 마스크로 따로 남긴다. **보간 셀 비율**을 적는다.
9. **평활화.** 3x3 중앙값 필터 한 번, 그 뒤 σ=1 셀 가우시안 한 번 (인자로 끌 수 있게). 평활화 전후의 보행 구간 z 표준편차를 적는다.
10. **메시.** 격자 정점을 셀마다 삼각형 둘로 잇는다(모두 삼각형 · Z 업 · 미터). trimesh 로 만들고 정점 · 면 수, 경계 상자, 워터타이트 여부(아니어도 됨)를 적는다. 면 수가 100,000 을 넘으면 `trimesh` 의 `simplify_quadric_decimation` 이 없을 수 있으므로 **격자 셀을 2 배로 키워 다시** 만든다. OBJ 로 저장한다.
11. **그림.** 높이 격자를 색으로, 관측 마스크를 회색으로, 카메라 궤적을 선으로 그린 PNG 를 `cv2` 로 만든다.
12. **연석 후보.** 보행 구간 z 히스토그램에서 두 봉우리가 있으면 그 차이를 «연석 높이 후보(추측)» 로 적는다. 없으면 «없음».

## 2. 통과 기준 (관문)

| 항목 | 기준 |
|---|---|
| 스크립트 | `poc_mesh_grid.py` 가 인자만으로 처음부터 끝까지 돈다. 머리에 분류 · 작성 · 근거 · 요지 도크스트링 |
| 메시 | `_out/mesh/ground.obj` 존재 · 면 수 100,000 이하 · 전부 삼각형 · Z 업 · 미터 |
| 관측 | 보행 구간 관측 셀 비율과 보간 셀 비율이 `ground_stats.json` 에 있다. 값의 좋고 나쁨은 판정하지 않는다 (처음 재는 것이다) |
| 평면 | 인라이어 수 · 잔차 RMS · 축척 배율 · 카메라 높이 중앙값이 있다 |
| 그림 | `ground_heightmap.png` 가 있고 궤적과 관측 마스크가 보인다 |

## 3. 하지 말 것

- 패키지 설치 금지 (pip · conda). isaac311 에 있는 것만 쓴다.
- 다른 프로세스를 죽이지 않는다. GPU 를 쓰지 않는다(전부 CPU numpy).
- `poc_mesh_grid.py` · `_out/mesh/` 밖의 저장소 파일을 만들거나 고치지 않는다. git 커밋 · 스테이징 금지.
- em dash(U+2014) 금지. 스크립트 주석 · 보고서 모두.

## 4. 보고서 형식 (`_out/mesh/REPORT-04.md`)

```
# REPORT-04 · 2.5D 지면 메시
## 환경   파이썬 · numpy · scipy · trimesh 버전 · 시작/끝 시각
## 명령   실제로 친 명령
## 결과   1절 각 단계의 수치 (읽은 점 · 거른 점 · 평면 인라이어/RMS · 축척 · 관측/보간 비율 · 평활화 전후 표준편차 · 정점/면 수 · 연석 후보)
## 산출물 경로 목록과 크기
## 문제   오류 원문 · 우회한 것 · 못 한 것
## 판정   2절 관문 항목별 통과/실패
```

`worker_done` 의 세 문장 요약에 면 수 · 보행 구간 관측 셀 비율 · 통과/실패를 넣고 `--report-path` 에 이 보고서 경로를 준다.
