# 중간 산물. 스크립트로 다시 만들어진다. 합쳐 400 MB 라 저장소에 안 넣는다.
cuts/
cuts4/
cutsB/
styled/
work/
workb/
test/
sheet/
sound/*.wav
keyvis/src/
keyvis/out/
worka/

# 오프닝 중간 산물. o1_raw.png 만 남긴다. build-opening.py 가 그것으로 다시 만든다.
opening/o1.png
opening/o2.png
opening/o2_raw.png
opening/opening-drift.mp4
foothold-launch-a-dry.mp4

# 힉스필드 생성물 원본. 343 MB 라 저장소에 안 넣는다.
# `python scripts/fetch-library.py` 로 URL 에서 다시 받는다. 크레딧이 안 든다.
# 목록(INDEX.md)과 프롬프트 전문(prompts.json)은 넣는다.
hf-archive/
library/*
!library/INDEX.md
!library/prompts.json
