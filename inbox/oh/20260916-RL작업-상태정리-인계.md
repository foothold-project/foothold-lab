# RL(A/평가) 작업 상태 정리와 인계 - 2026-09-16

> 분류: 운영
> 작성: 오현민 · 2026-09-16 10:05
> 근거: 실측 (2026-09-16 09:51 KST `gh issue view` 로 9건 본문·코멘트 재조회 · origin/main 00278e5 의 `sim/eval/` grep · 개인 작업 폴더 대조)
> 요지: 담당 이슈 7건(#67 #293 #297 #298 #299 #300 #28)이 어디까지 됐고 무엇이 남았는지, 산출물이 어디 있는지를 한 자리에 적는다. 2026-09-10 부로 작성자는 인지(B/인지) 쪽으로 중심을 옮겼고 아래 RL 항목은 더 진행하지 않는다
> 상태: 초안

## 0. 먼저

- **역할 문서(`docs/ROLES.md` 2026-09-10 판)는 아직 `A/평가` 주담당을 작성자로 적고 있습니다.** 이 문서는 그 배정을 바꾸는 문서가 아닙니다. 작업을 멈추는 사실과 자료 위치를 남기는 것이고, 배정 변경은 팀장 판단입니다 `확인됨`
- 결과 제출 2건은 별도 파일입니다: `20260916-지형혼합-arm-평가57회.md`(#292) · `20260916-징검다리-발접촉-정적시험.md`(#332)
- 「팀장이 `sim/eval/` 로 반영 여부 판단」 항목(#297 #298 #299 #300 공통)의 현재 사실: 제출 문서는 2026-09-14 에 `docs/research/평가하네스-수정본.md` 로 **승격**됐습니다(#331 닫힘). **코드는 정본 `sim/eval/` 에 들어가지 않았습니다.** origin/main `00278e5`(2026-09-16) 기준 `sim/` 아래에서 `_eps = hs` · `terrain_audit` · `--terrain_cfg` · `cli_absolute` · `"horizontal_scale"` · `[AUDIT] OK` 여섯 문자열이 전부 0건입니다 `실측`. 그 사이 팀장이 PR #394 로 `sim/eval/` 을 크게 고쳤습니다(CSV 24열에서 27열, 이후 기본 59열. 27열 상수 `LEGACY_RAW_COLUMNS` 는 남아 있으나 이를 고르는 CLI 옵션은 아직 없음). 제출본은 09-08 판 하네스 기준이라 지금 정본에 그대로 얹히지 않을 수 있습니다 `미확인`

## 1. 이슈별 상태

### #67 실패 지형 stepping_stones: 진단 -> 레시피

| 완결조건 | 상태 |
|---|---|
| 0단계 관문(틈이 height scan 에 보이는가)을 닫았다 | **못 닫음.** 「14 cm 틈이 0.1 m 격자 스캔에 보이는가」를 직접 잰 적이 없습니다. 대신 height_scan 위조 4조건(2026-09-08)에서 스캔을 지워도 정지가 거의 그대로였고(75/106 vs 76/109), 평지에 가짜 구멍을 넣어도 100/100 통과였습니다. 「관측이 원인이 아니다」 쪽을 강하게 지지하지만 CI 가 겹쳐 확정은 아닙니다 `미확인` |
| 지형 진단 1장 | **팀 형식 1장은 미작성.** 재료는 있습니다: 57회 arm 비교(#292 제출), 미세 틈·턱·보폭 스윕(#291, 하네스 제출본 `DATA.md`), 발 접촉 정적 시험(#332 제출), 위조 4조건. 실패 모드는 「낙상 60~75 % + 입구 정지 25~40 %, 정지는 거의 완전 정지」까지 확인됐습니다 `확인됨` |
| 레시피 1장 | **미작성.** 시도한 것: rough 0.6 + 징검다리(hs 0.05, 실현 틈 0.05 m, 돌 0.50 m) 0.4 · 학습률 1e-4 · 무진전 종료를 얹은 6,000 it 파인튜닝 1런(2026-09-10, 정상 완주), 그리고 「틈만 / 높이만」 절제 2런. 절제에서 **틈만 넣은 A 조건의 낙상이 58 에서 28 로 내려가 기준선과 CI 가 갈렸고**, 높이만 넣은 B 는 갈리지 않았습니다(n=100, 20초·4 m 판정이라 팀 정식 판정 아님) `확인됨`. 6,000 it 런은 저장 설정에서 `curriculum: false` 였음이 뒤늦게 확인돼 난이도 지표 해석은 전부 재검토 대상입니다 `확인됨`. 한 번에 하나만 바꾼 정식 레시피 실험은 하지 않았습니다 |
| 다섯 장의 형식이 같다 | 해당 없음(1장이 없음) |

산출물: 개인 작업 폴더 `experiments/20260908_forge/`(위조 4조건) · `experiments/20260910_stones-finetune/`(6,000 it 런, `RECIPE.md` · `raw/train_metrics.csv`) · `experiments/20260910_stones-ablation/DESIGN.md`(절제 설계) · 개인 위키 연구 기록(징검다리 · 절제 · 위조 4조건 페이지, 참고용).
인계: **가능.** 원자료와 인계서가 폴더째 있습니다. 필요하면 `inbox/oh/` 로 올립니다.

### #293 평가 코드 조용한 오작동 4건 기록과 재발 방지

| 완결조건 | 상태 |
|---|---|
| 4건이 팀 문서 또는 위키에 반영 | **미완료.** 1·3번은 이슈 본문에만 있고 팀 문서에는 들어가지 않았습니다. 4번(hs 다른 실행 비교 금지)은 정본 `go2-spec-sim-vs-real.md` v1.1 §3-1 과 승격된 `평가하네스-수정본.md` 에 들어갔습니다. 2번(환경 시드)은 #138 이 아직 OPEN 이라 「절차 한 줄」을 받아 적지 못했습니다 `확인됨` |
| 1번 처방(로딩 경로 + SHA256 을 manifest 에 기록)이 하네스에 들어감 | **제출본에는 있음** (`play_eval.py` 의 manifest `checkpoint.sha256` · `checkpoint.source = cli_absolute`). 정본 `sim/eval/` 에는 없음 `실측` |
| 3번이 RunPod 안내 문서에 반영 (#108) | #108 OPEN. 팀장 판단 항목, 작성자는 손대지 않았습니다 |
| 4번 처방(`hs` 를 manifest 에서 읽어 표 생성기에 넘김) | **제출본에는 있음** (`play_eval.py` manifest `terrain.horizontal_scale` · `analysis/sweep_table.py` 가 그 값을 읽음). 정본에는 없음 `실측` |

인계: **가능.** 코드는 승격된 제출본 폴더 `inbox/oh/20260908-평가하네스-수정본/` 에 그대로 있습니다.

### #297 지형 검사 관문이 검사 실패를 못 막는다 · 러너 종료코드 계약

| 완결조건 | 상태 |
|---|---|
| 수정본 제출 | 완료 (PR #326 머지, 2026-09-08) |
| 팀장이 `sim/eval/` 반영 판단 | 문서 승격됨 · 코드 미반영 (0절) |
| 일부러 실패시켜 러너가 멈추는 로그 | 완료 (2026-09-09 09:32 코멘트) |
| 제출본과 기록이 일치하는지 대조 | **대조했고 불일치를 찾았습니다.** 제출본 `run_audits.sh` 는 종료코드를 담기만 하고 판정은 `[AUDIT] OK` 문자열만 봅니다. 주석과 코드가 어긋납니다. 한 줄 수정안(`[ "$E" -eq 0 ] && grep -q ...`)을 같은 코멘트에 적었고, **제출본 자체는 고치지 않았습니다** `확인됨` |

관련: #350(하네스가 죽어도 exit 0)은 OPEN 이고 우회 방법 실측 코멘트 1건이 달린 확인 단계입니다. #364(종료코드 계약 문서화)는 OPEN 이고 코멘트 0건, 정본 문서에는 아직 반영되지 않았습니다. 이 이슈의 남은 몫은 그 둘의 결정을 따르면 됩니다.
산출물: 재현 로그 `experiments/20260908_gate297/`(`REPRO_redacted.log` · `audit_FAIL.log` · `audit_PASS.log`). 인계 **가능.**

### #298 부동소수점 절단으로 지형 치수가 틀어진다 · round 스냅

| 완결조건 | 상태 |
|---|---|
| 수정본 제출 | 완료 (PR #326, `terrain_cfg.py` 의 `_eps = hs * 1e-6` + `round`) |
| 팀장 `sim/eval/` 반영 판단 | 문서 승격됨 · 코드 미반영 (0절) |
| 요청값 대비 실제 생성값 대조표 (틈 7점 · 턱 전 구간) | **PR #326 코멘트(2026-09-09 10:57)에 게재.** 23조건 사후 감사에서 요청 7.5 cm = 3 px 등 전행 일치. 단 「이 결함이 든 스윕으로 이미 낸 결과」의 역사적 영향은 **미확인**으로 적었습니다(감사가 사후 재생성이라 09-08 23런과 바이트 동일성을 잇지 못함) `확인됨` |
| 요청 치수와 실제 치수를 manifest 에 둘 다 기록 | **미구현.** 제출본 `play_eval.py` 의 manifest `terrain` 항목에는 `horizontal_scale` · `sub_terrains`(생성기 인자 원값)만 있고 요청값·실현값(`realized_*`)을 따로 적는 키가 없습니다. 실현 틈은 `terrain_cfg.py` 의 스냅 계산과 검사기 JSON 으로만 되짚을 수 있습니다 `실측` |

인계 **가능.**

### #299 지형 검사기 신뢰성 2건 · hs 미전파와 생성기 하드코딩

| 완결조건 | 상태 |
|---|---|
| 수정본 제출 | 완료 (PR #326, `terrain_audit.py` 가 `cfg.horizontal_scale = gen.horizontal_scale` 대입 · `cfg.function` 을 따라감) |
| 팀장 `sim/eval/` 반영 판단 | 문서 승격됨 · 코드 미반영 (0절) |
| hs 0.025 에서 틈 2.5/5/7.5 cm 가 검사 보고에 그 값으로 나옴 | 사후 감사 근거는 PR #326 코멘트(2026-09-09)에 있음(마스크 구멍 폭 1/2/3/4/6/8 px 전행 일치). 검사기 **보고 출력**에서 그 값을 확인한 로그는 따로 남기지 않았습니다 `미확인` |
| 커스텀 생성기 cfg 로 검사기가 그 생성기를 호출함 | 코드 반영은 됐으나 **실행으로 확인한 기록 없음** `미확인` |
| 검사기가 「무엇을 어떤 스케일로」 검사했는지 첫 줄에 찍음 | JSON 출력에 `horizontal_scale_m` 이 들어가고 마지막 줄에 `[AUDIT] OK` 를 찍습니다. **첫 줄 출력은 미구현** `실측` |

인계 **가능.**

### #300 play_eval.py 스윕 지형 선택 스위치 (--terrain_cfg)

| 완결조건 | 상태 |
|---|---|
| 수정본 제출 | 완료 (PR #326, `play_eval.py --terrain_cfg`) |
| 팀장 `sim/eval/` 반영 판단 | 문서 승격됨 · 코드 미반영 (0절) |
| 인계 문서의 스윕 명령이 그대로 실행됨 | 09-08 틈·턱 스윕 23/23 이 이 스위치로 돌았습니다 `확인됨`. 다만 「인계 문서에 적힌 명령 그대로」를 별도로 재실행해 확인한 기록은 없습니다 `미확인` |
| manifest 에 `terrain_cfg` 가 남음 | 제출본은 별도 키가 아니라 `terrain.switch` 에 `"terrain_cfg:<이름>"` 문자열로 남깁니다 `실측`(코드 확인). 09-08 스윕의 실제 manifest 는 노트북에 회수돼 있지 않아(57회 manifest 만 있음, 그쪽은 `switch = stepping_stones`) 파일로는 확인하지 못했습니다 `미확인`. 확인 방법: Pod 또는 백업의 스윕 run 폴더 `manifest.json` 에서 `terrain.switch` 값 |
| 인계 문서의 명령 예시를 실제 문법으로 갱신 | 제출본 `HARNESS.md` 에 `--terrain_cfg` 가 1곳 있습니다 `실측`. 그 예시가 「그대로 실행되는 문법」인지 재실행으로 확인한 기록은 없습니다 `미확인` |

인계 **가능.**

### #28 RoboGauge 공개 정책 4종: 우리 평가에 붙이기

- 2026-08-28 팀장 코멘트로 목적이 바뀌었습니다: 45차원 증류는 죽었고, 남은 쓰임은 **대조군(실패 지형 지도)**입니다. 남은 3항목(4종을 10종 지형에 태우기 · 실패 지형 지도 · 벤치마크 문서에 대조군 절 추가)은 담당 `A/평가`, 관문 9/30 입니다 `확인됨`
- **작성자는 이 3항목에 착수하지 않았습니다.** 08-28 이후 작성자 쪽 작업 기록이 없습니다 `확인됨`
- 실행 재료는 팀장 쪽에 다 있습니다(관측 매핑 확정, 4종 로드·실행 검증 완료, 관절 재배열 규칙). 작성자가 만든 것은 없어 인계할 자료가 없습니다
- 제안: 작성자가 이 이슈를 더 진행하지 않으므로 **담당 재배정 또는 닫기**를 팀장이 판단해 주십시오. 관문 9/30 전에 대조군이 필요하면 재배정이 맞고, 아니면 닫는 것이 맞습니다. 판단은 팀장 몫입니다

## 2. 이슈별 코멘트 문안 (그대로 붙일 수 있게)

### #67 에 붙일 문안

```
### 상태 보고 (2026-09-16) - 여기까지 하고 멈춥니다

2026-09-10 부로 인지(B/인지) 작업으로 중심을 옮겨 이 이슈는 더 진행하지 않습니다. 된 것과 안 된 것을 남깁니다.

| 완결조건 | 상태 |
|---|---|
| 0단계 관문 | 못 닫음. 「틈이 스캔에 보이는가」 직접 측정 없음. height_scan 위조 4조건(09-08)이 「관측이 원인이 아니다」 쪽을 강하게 지지하나 CI 겹침 |
| 진단 1장 | 미작성. 재료는 #292(57회) · #291(스윕) · #332(발 접촉) 제출물에 있음. 실패 모드는 낙상 60~75 % + 입구 정지 25~40 % |
| 레시피 1장 | 미작성. rough 0.6 + 징검다리 0.4 · lr 1e-4 · 무진전 종료 6,000 it 1런(복합 런, curriculum false 였음) · 틈만/높이만 절제 2런(틈만 조건 낙상 58 -> 28, CI 분리) 까지 |
| 형식 통일 | 해당 없음 |

원자료·인계서는 개인 작업 폴더에 폴더째 있습니다. 필요하면 `inbox/oh/` 로 올리겠습니다. 담당 재배정은 팀장님 판단에 맡깁니다.
```

### #293 에 붙일 문안

```
### 상태 보고 (2026-09-16)

| 항목 | 상태 |
|---|---|
| 4건 문서 반영 | **미완료.** 1·3 은 이슈 본문에만 있고 팀 문서 미반영. 4 는 정본 `go2-spec-sim-vs-real.md` §3-1 과 `평가하네스-수정본.md`(09-14 승격). 2 는 #138 결론 대기 |
| 1번 처방 (manifest 에 경로+SHA256) | 제출본 `play_eval.py` 에 있음. 정본 `sim/eval/` 미반영 |
| 3번 RunPod 문서 | #108 OPEN, 팀장 판단 |
| 4번 처방 (hs 를 manifest 에서 표 생성기로) | 제출본에 있음. 정본 미반영 |

2026-09-10 부로 인지 작업으로 옮겨 이후 진행하지 않습니다. 코드는 승격된 제출본 폴더에 그대로 있습니다.
```

### #297 에 붙일 문안

```
### 상태 보고 (2026-09-16)

- 「제출본과 기록 대조」: 대조했고 불일치를 찾았습니다(09-09 코멘트). 제출본 `run_audits.sh` 는 종료코드를 판정에 쓰지 않습니다. 한 줄 수정안을 적었고 제출본은 고치지 않았습니다.
- 남은 것은 정본 반영입니다. #331 은 문서만 승격되고 닫혔고 코드 반영 결정은 기록되지 않았습니다. #350 은 우회 방법 실측 코멘트가 달린 확인 단계, #364 는 코멘트 없이 열려 있고 정본 문서 미반영이라 그 둘의 결정을 따르면 됩니다.
- 재현 로그는 개인 폴더에 있고 필요하면 `inbox/oh/` 로 올립니다. 2026-09-10 부로 인지 작업으로 옮겨 이후 진행하지 않습니다.
```

### #298 에 붙일 문안

```
### 상태 보고 (2026-09-16)

- 대조표(틈 7점 · 턱): PR #326 코멘트(09-09)에 게재. 23조건 사후 감사 전행 일치. 역사적 영향은 미확인으로 남김.
- 「요청·실현 치수를 manifest 에 둘 다 기록」: 제출본에 미구현입니다. manifest `terrain` 항목에 요청값·실현값 키가 없습니다.
- 정본 `sim/eval/` 반영: #331 은 문서만 승격되고 닫혔고, 코드 반영 결정은 기록되지 않았습니다. 2026-09-10 부로 인지 작업으로 옮겨 이후 진행하지 않습니다.
```

### #299 에 붙일 문안

```
### 상태 보고 (2026-09-16)

- 코드 반영(hs 전파 · `cfg.function` 추종)은 PR #326 제출본에 있음. 정본 미반영.
- 「검사 보고에 2.5/5/7.5 cm 가 그 값으로」: 사후 감사 근거는 PR #326 코멘트에 있으나 검사기 출력 로그로는 따로 남기지 않음(미확인).
- 「커스텀 생성기 호출 확인」 실행 기록 없음(미확인). 「첫 줄에 스케일 출력」 미구현(JSON 에 `horizontal_scale_m` 만).
- 2026-09-10 부로 인지 작업으로 옮겨 이후 진행하지 않습니다.
```

### #300 에 붙일 문안

```
### 상태 보고 (2026-09-16)

- `--terrain_cfg` 스위치는 PR #326 제출본에 있고, 09-08 스윕 23/23 이 이 스위치로 돌았습니다. 정본 미반영.
- manifest 기록은 별도 키가 아니라 `terrain.switch = "terrain_cfg:<이름>"` 문자열입니다. 스윕 manifest 실물로는 확인하지 못했습니다(미확인). `HARNESS.md` 에 `--terrain_cfg` 예시는 1곳 있습니다.
- 2026-09-10 부로 인지 작업으로 옮겨 이후 진행하지 않습니다.
```

### #28 에 붙일 문안 (종료 «제안». 닫는 것은 팀장)

```
### 종료 또는 재배정 제안 (2026-09-16)

08-28 코멘트로 목적이 「대조군(실패 지형 지도)」로 바뀐 뒤 남은 3항목은 제 담당인데, 착수하지 않았고 2026-09-10 부로 인지(B/인지) 작업으로 옮겨 이후에도 진행하지 않습니다. 제가 만든 자료는 없습니다(재료는 팀장님 쪽 매핑·검증에 다 있습니다).

관문 9/30 전에 대조군이 필요하면 재배정을, 아니면 닫기를 판단해 주십시오. 닫기는 팀장님 판단에 맡깁니다.
```

## 3. 참고한 곳

- 이슈 [#67](https://github.com/foothold-project/foothold-lab/issues/67) · [#293](https://github.com/foothold-project/foothold-lab/issues/293) · [#297](https://github.com/foothold-project/foothold-lab/issues/297) · [#298](https://github.com/foothold-project/foothold-lab/issues/298) · [#299](https://github.com/foothold-project/foothold-lab/issues/299) · [#300](https://github.com/foothold-project/foothold-lab/issues/300) · [#28](https://github.com/foothold-project/foothold-lab/issues/28)
- PR [#326](https://github.com/foothold-project/foothold-lab/pull/326) (하네스 제출, 머지) · 승격 검토 [#331](https://github.com/foothold-project/foothold-lab/issues/331) (2026-09-14 승격) · PR [#394](https://github.com/foothold-project/foothold-lab/pull/394) (직후 27열 · 현재 기본 59열, 27열 상수 `LEGACY_RAW_COLUMNS` 만 잔존 · CLI 옵션 없음)
- 관련 [#364](https://github.com/foothold-project/foothold-lab/issues/364) · [#350](https://github.com/foothold-project/foothold-lab/issues/350) · [#138](https://github.com/foothold-project/foothold-lab/issues/138) · [#108](https://github.com/foothold-project/foothold-lab/issues/108)
- 정본 `docs/ROLES.md` (2026-09-10 판) · `docs/research/평가하네스-수정본.md` · `docs/research/20260908-go2-spec-sim-vs-real.md` v1.1
