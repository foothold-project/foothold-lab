# 평가 하네스 원본 추적

> 분류: 운영
> 작성: 오흥재 · 2026-09-02 15:20
> 근거: 실측
> 요지: 이 폴더의 하네스가 어디서 왔고, 무엇이 복원물이고 무엇이 우리 변경인지 가른다.
> 상태: 확정
> 판: v1.3
> 이슈: #125

## 1. 한 줄

**두 프로젝트 문서에서 바이트 단위로 동일하게 복원한 Candidate 스냅샷이다.
원래 RunPod 실행 파일은 회수하거나 직접 대조하지 못했으므로,
provenance 가 닫히기 전까지 검증된 런타임 정본으로 취급하지 않는다.**

## 2. 실행본을 찾지 못했다 `확인됨`

`benchmark-setup-lim.md` 가 가리키는 실행 경로는 RunPod 컨테이너 안입니다.

```
/workspace/isaaclab/scripts/reinforcement_learning/rsl_rl/eval_generalization.py
```

2026-09-02 에 다음을 전수로 뒤졌고 실행본은 없었습니다.

| 뒤진 자리 | 결과 |
|---|---|
| Windows 로컬 `Desktop/jay/인공지능사관학교/` 하위 | 하네스 파일명 0건 |
| WSL Ubuntu-24.04 `find / -xdev` | `/workspace` 부재 · `*isaaclab*` 0건 |
| Docker Desktop | 데몬 정지. 조회 불가 |
| `foothold-go2` 저장소 | `.py` 파일 0건 |
| `foothold-lab` git 전 이력 | 하네스 파일명 0건. 삭제 이력도 없음 |

`docs/research/benchmark-repro-protocol.md` §1-2 · §2-0단계가 같은 상태를 8/22 에 이미
적어 두었습니다. 「벤치마크 스크립트 원본이 재현의 전제 · 실행자에게 원본과 커맨드라인 요청」.

## 3. 복원한 것 · 복원한 방법

| 출처 | sha256 (문서) |
|---|---|
| `inbox/lim/20260822-베이스라인 모델의 간단한 미경험 험지 성능 측정.md` | `ece95275a8510089…` |
| `docs/research/benchmark-setup-lim.md` | `05b4233495a18dc0…` |

두 문서가 품은 `cat > ... <<'PY'` 히어독을 풀었습니다.
히어독은 마지막 줄에도 개행을 붙이므로 그것까지 맞췄습니다.

| 파일 | 크기 | 행 | sha256 |
|---|---|---|---|
| `eval_generalization.py` | 19,991 B | 652 | `1e39567a96c63d4e8219f16c65f959661326e5ce331039e73f5a3de8e4acc541` |
| `generalization_env_cfg.py` | 4,740 B | 148 | `054f00cc3c4013d7dabcccf2a004b977e93c5c999e0117fad08306b434c1a521` |
| `record_generalization.py` | 6,598 B | 246 | `bf95be21e377cdac9987b01e8bbdf9a2227c93b086aefec894af9842707bbca2` |
| `record_all_generalization.sh` | 575 B | 24 | `f6cdd054b7fc33288ef467670c49f7726cb3e66d75cbe1ef59881345194fbd56` |

**두 문서에서 나온 넷이 모두 바이트 단위로 같습니다** `확인됨`.
승격이 코드를 변형하지 않았다는 뜻입니다.

다시 뽑아 대조하는 것은 표준 라이브러리만으로 됩니다.

```bash
python sim/eval/provenance/extract_candidate.py --check
cd sim/eval/provenance/candidate-20260822 && sha256sum -c SHA256SUMS
```

이 저장소는 `core.autocrlf=true` 라 기본값대로면 체크아웃 때 CRLF 가 되어 해시가 깨집니다.
그래서 `sim/eval/provenance/.gitattributes` 에 `* text eol=lf` 를 걸었습니다.
루트 `.gitattributes` 는 건드리지 않았습니다.

## 4. 실행본과의 정합 · 간접 증거뿐이다 `추측`

직접 대조가 불가능하므로 산술로 맞춰 봤습니다.

| 증거 | 코드블록에서 계산 | 기록된 실측 |
|---|---|---|
| `gap` 틈 폭 | `0.15 + 0.5 × (0.40 - 0.15) = 0.275 m` | 27.5 cm |
| 이상 거리 | `0.5 m/s × 6.0 s = 3.0 m` | `ideal_distance_m` 3.0 |
| 판정 축 | survival · progress · tracking · direction + overall | 5축 |
| env 수 | `len(TERRAIN_NAMES) = 10` · 1행 10열 | env 10 |
| 좌우 이탈 계산 위치 | 추출본 487행 | 문서 837행 근처 |

**판정: 500판을 낸 그 코드와 같거나 극히 가까운 판본이다 `추측`.**
확정하려면 임석헌의 팟 원본 또는 실행 커맨드라인이 필요합니다.

## 5. provenance 를 닫는 조건

셋 중 하나면 닫힙니다.

1. 팟 원본 파일을 받아 `provenance/runpod-<날짜>/` 에 두고 해시가 일치
2. 실행 커맨드라인과 실행 로그를 받아 인자 · 관측 차원 · 지형 매핑이 일치
3. 같은 체크포인트로 이 스냅샷을 돌려 `generalization_raw.csv` v2 가 재현

닫히기 전까지 이 하네스로 낸 수치에는 **「Candidate 하네스 기준」**을 함께 적습니다.

## 6. 복원과 우리 변경의 경계

| 커밋 | 무엇 | 성격 |
|---|---|---|
| 0 | Candidate 스냅샷 · 추출기 · 이 문서 | **복원. 기능 변경 0** |
| 1 이후 | `sim/eval/` 활성본 | **우리 변경** |

활성본은 커밋 0 의 스냅샷에서 갈라져 나옵니다.
`provenance/candidate-20260822/` 는 그 뒤로 **고치지 않습니다.**

`record_generalization.py` 와 `record_all_generalization.sh` 는 **보존만** 합니다.
#125 활성 범위가 아니고, 정식 실행 경로로 승격하지도 않습니다.

## 7. 활성본이 스냅샷에서 갈라진 자리

커밋 1 부터 여기에 한 줄씩 쌓습니다.

| 커밋 | #125 항목 | 무엇이 달라졌나 |
|---|---|---|
| 1 | (없음) | **동작 변경 없음.** 스냅샷의 판정·집계 식을 `metrics.py` 순수 함수로 옮겨 적었다. 스냅샷은 손대지 않았다. `tests/` 가 스냅샷 원문을 다시 읽어 같은 값이 나오는지 고정한다 |
| 2 | 1번 | **열 하나 추가.** `peak_lateral_drift_m` (스텝별 최대 좌우 이탈). 관측·진단용이고 **판정 5축은 손대지 않았다.** 스냅샷과 처음으로 달라지는 자리 |
| 3 | 2번 | **지형 하나 추가.** `flat` 을 `sub_terrains` **맨 뒤에**. `num_cols` 10 -> 11 · `num_envs` 10 -> 11. 기존 10종의 설정과 인덱스는 한 글자도 안 바뀌었다 |

### 커밋 3 · 평지 지형 추가

활성 설정 `generalization_env_cfg.py` 가 스냅샷에서 갈라졌습니다.
**다른 것은 셋뿐입니다.**

| 무엇 | 스냅샷 | 활성본 |
|---|---|---|
| `sub_terrains` | 험지 10종 | 10종 + `flat` **맨 뒤** |
| `num_cols` | 10 | 11 |
| `scene.num_envs` | 10 | 11 |

**왜 맨 뒤인가.** Isaac Lab 은 `sub_terrains` 등록 순서대로 열을 배정하고,
하네스는 `TERRAIN_NAMES[terrain_type]` 로 이름을 되찾습니다. `flat` 을 앞에 넣으면
기존 10종의 지형 인덱스가 전부 한 칸씩 밀려 **500판 기록과 대조가 끊깁니다.**
`tests/test_terrains.py` 가 기존 10종의 순서 · 인덱스 · 설정 원문을 항목 단위로 대조합니다.

**어느 클래스인가.** `MeshPlaneTerrainCfg` 입니다. 팀 지형 조사
(`inbox/lim/20260813-terrain_guide.md` 74행)가 Isaac Lab v2.3.2 에서
「generator 용 평평한 sub-terrain」으로 확인해 둔 클래스입니다 `확인됨`.

**이름 목록을 따로 뺐습니다.** `terrains.py` 가 순서의 정본이고 `isaaclab` 을
쓰지 않아 Windows 에서 그대로 임포트됩니다. 설정과 목록이 어긋나면
이름과 지형이 뒤바뀌므로 시험이 둘을 매번 대조합니다.

| 보장한다 | 보장하지 않는다 |
|---|---|
| 기존 10종의 설정 원문이 스냅샷과 같다 | Isaac 이 이 설정으로 무엇을 굽는지 |
| 기존 10종의 인덱스가 안 밀렸다 | `MeshPlaneTerrainCfg` 가 팟에서 실제로 임포트되는지 |
| 늘어난 것이 `flat` 하나뿐이다 | 열 배정이 정말 등록 순서를 따르는지 |
| 이름 목록과 설정 순서가 같다 | 비율 정규화 뒤 칸이 하나씩 돌아가는지 |
| `num_rows` 와 `difficulty_range` 를 안 건드렸다 | |
| 타일 크기를 안 건드렸다 | |

`isaaclab` 이 없어 **`ast` 로 구조만 봅니다.** 「설정이 무엇이라고 적혀 있는가」이지
「Isaac 이 그것으로 무엇을 만드는가」가 아닙니다.

### 짚어 둘 것 · 8 m 타일과 10 m 기준이 안 맞는다 `추측`

`size=(8.0, 8.0)` 입니다. 로봇은 타일 중앙에서 출발하므로 전방으로 **약 4 m** 를
가면 sub-terrain 을 벗어나 `border_width=10.0` 의 **평평한 테두리**로 들어갑니다.

기록된 500판은 3 m (0.5 m/s × 6 s) 라 타일 안에 있었습니다.
새 기준 10 m 는 **뒤쪽 6 m 가 지형이 아닙니다.**

| 지형 | 10 m 기준에서 |
|---|---|
| `flat` | 테두리도 평지라 **문제 없음** |
| 험지 10종 | 앞 4 m 만 지형. **뒤 6 m 는 평지** |

`flat` 기준선은 지금 설정으로 잴 수 있습니다. **험지 10종 재측정은 못 잽니다.**
고치려면 `size` 를 키우거나 출발점을 타일 가장자리로 옮겨야 하고, 둘 다
**기존 10종 전부에 영향을 줍니다.** 그래서 이 커밋에서 건드리지 않았습니다.
`tests/test_terrains.py` 가 `size` 와 `border_width` 를 못 박아,
누가 바꾸면 먼저 걸리게 해 두었습니다.

행이 x 축이고 열이 y 축이라는 것은 `추측` 입니다. 팟에서 `env_origins` 를
한 번 찍으면 확정됩니다.

### 커밋 2 · 스냅샷과 처음 달라지는 자리

**달라지는 것은 열 하나뿐입니다.** `metrics.ADDED_COLUMNS` 에 그 목록이 있고,
시험이 그 목록 밖으로 벗어나지 못하게 막습니다.

| | 스냅샷 | 커밋 2 이후 |
|---|---|---|
| 원시 CSV 열 | 19 | **20** (`lateral_drift_m` 바로 뒤에 하나) |
| 판정 5축 | 끝점 이탈로 방향 판정 | **그대로** |
| 요약 CSV 열 | 13 | 그대로 |

**왜 더하나.** 끝점 변위는 경로를 보지 않습니다. 옆으로 크게 밀렸다가 돌아오면
끝점 이탈은 작고 「방향을 지켰다」로 셉니다. 그 흔들림이 기록에 남지 않습니다.
`docs/` 지표 표가 이 값을 오래 「누적·적분」이라 적어 온 것도 같은 오해에서 나왔습니다
(#125 7번 · `inbox/jay/20260902-benchmark-setup-lim-지표표-정정안.md`).

**왜 판정을 바꾸지 않나.** 멘토 기준이 끝점입니다. 판정을 최댓값으로 바꾸면
기존 500판과 새 측정이 비교 불가능해집니다. 최댓값은 **관측용**입니다.
시험이 이것을 못 박습니다. 중간에 문턱 0.75 m 를 넘겼다가 끝점이 0.05 m 로 돌아온 판에서
`direction_success` 가 여전히 `True` 인지 봅니다.

**이름을 `max` 가 아니라 `peak` 으로 둔 이유.** 하네스에 이미 `--max_lateral_drift`
라는 **문턱값**이 있습니다. 관측값을 `max_lateral_drift_m` 이라 부르면 CSV 열과
인자가 한 글자 차이가 되어 「잰 값인가 기준값인가」가 헷갈립니다.
`#125` 본문의 말은 「최대 좌우 이탈」이고 뜻은 그대로입니다. 이름만 갈랐습니다.

| 보장한다 | 보장하지 않는다 |
|---|---|
| 판정 5축이 스냅샷과 같다 | 실제 하네스가 매 스텝 위치를 어떻게 표본하는지 |
| 새 열은 끝점 이탈보다 작지 않다 | 표본 간격이 최댓값에 얼마나 영향을 주는지 |
| 좌우 부호가 최댓값을 바꾸지 않는다 | float32 반올림 |
| 새 열이 `ADDED_COLUMNS` 하나뿐이다 | |
| 새 열의 CSV 자리가 고정된다 | |

**표본 간격은 GPU 팟에서 정해집니다.** 여기서 고정한 것은 「주어진 표본들에서
최댓값을 어떻게 집는가」이지 「얼마나 자주 표본하는가」가 아닙니다.

### 커밋 1 이 무엇을 보장하고 무엇을 보장하지 않나

`tests/snapshot_oracle.py` 는 스냅샷 파일에서 **판정 블록(473~541행)과
`summarize_results` 함수를 원문 그대로 잘라 와** 실행합니다. 다시 쓰지 않습니다.
스냅샷이 쓰는 텐서 연산만 평면 2성분 대역으로 갈아 끼웁니다.

| 보장한다 | 보장하지 않는다 |
|---|---|
| 식과 제어 흐름이 같다 | float32 반올림까지 같다 |
| 경계 조건(`>=` `<=`)이 같다 | 실제 텐서 · 장치 · 정책이 같은 값을 준다 |
| CSV 열 순서가 같다 | 지형 생성과 env 매핑이 같다 |
| 지형별 집계가 같다 | |

실제 하네스는 float32 텐서로 계산합니다. **비트 단위 일치는 GPU 팟에서만 확인됩니다.**

시험이 정말 무언가를 잡는지 돌연변이 6종으로 확인했습니다 `확인됨`.
경계 부호 뒤집기 · 측면축 부호 뒤집기 · 열 순서 뒤바꾸기 · 0 나눗셈 방지 제거 ·
`abs` 제거 · 판정축 하나 빼기. **여섯 전부 시험이 잡았습니다.**

## 판 이력

| 판 | 언제 | 무엇이 바뀌었나 | 근거 |
|---|---|---|---|
| **v1.3** | 2026-09-02 | §7 에 커밋 3 분기 기록. 평지 지형과 8 m 타일 문제 | #125 |
| v1.2 | 2026-09-02 | §7 에 커밋 2 분기 기록. 스냅샷과 처음 달라지는 열과 그 이유 | #125 |
| v1.1 | 2026-09-02 | §7 에 커밋 1 분기 기록. 보장 범위와 돌연변이 시험 결과 | #125 |
| v1.0 | 2026-09-02 | 처음 씀. Candidate 스냅샷 복원 | #125 |
