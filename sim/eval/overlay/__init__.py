"""영상 위에 계측값을 겹쳐 그리는 후처리 도구. #99 · 발표 자산.

**이 `__init__` 은 비어 있습니다.** 일부러 그렇습니다.

`trace.py` 는 `record_flat_baseline.py` 가 Isaac Lab 안에서 부릅니다. 그 자리에는
Pillow 도 PyAV 도 없을 수 있고, 있어도 Kit 프로세스에 새 네이티브 라이브러리를
들이고 싶지 않습니다. 그래서

* `trace.py` 는 **표준 라이브러리만** 씁니다. Isaac 쪽에서 임포트해도 안전합니다.
* `hud.py` 는 Pillow 를, `render.py` 는 PyAV 를 씁니다. **후처리 쪽에서만** 씁니다.

여기서 무언가를 임포트하면 그 구분이 무너집니다. 비워 두십시오.
"""
