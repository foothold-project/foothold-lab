# AGENTS.md · 이 저장소에서 AI 에이전트가 지켜야 할 것

> **이 파일은 사람이 아니라 AI 가 읽는 규칙서입니다.**
> Cursor · Codex · Claude Code 같은 도구가 작업을 시작할 때 자동으로 읽습니다.
> 팀원 각자가 쓰는 AI 가 달라도, **여기 적힌 것은 공통으로 지킵니다.**
>
> 이 파일에는 **공통 규칙만** 둡니다. 개인 작업 방식은 이 파일에 적지 말고,
> `inbox/<이름>/AGENTS.md` 를 만들거나 각자 로컬 설정(gitignore 대상)으로 두십시오.
> 에이전트마다 쓰는 도구가 달라, 공통 파일에 개인 설정이 섞이면 다른 사람의
> 에이전트가 그것을 규칙으로 읽습니다.

---

> **흐름 전체를 한 장으로 보려면** `docs/WORKFLOW.md` 를 보십시오.
> 이슈를 만드는 것부터 머지까지의 순서와, 무엇이 자동으로 도는지가 표로 있습니다.

## 0. 작업을 시작하기 전에 반드시

```bash
git pull origin main
```

**이 한 줄을 빠뜨리면 남의 작업이 사라집니다.** 규칙이 아니라 사고 기록입니다.

2026-08-25 와 08-26 에 같은 일이 두 번 났습니다. 옛 `main` 에서 갈라진 브랜치로
PR 을 올렸더니, 그 사이 팀장이 복원해 둔 회의록 5건이 **10,317자에서 2,335자로**
되돌아가는 내용이 됐습니다. 없앴던 인쇄 시스템도 되살아났습니다.

git 이 충돌을 내서 막았지만, **충돌이 안 났으면 조용히 덮였습니다.**

> 작업이 하루 이상 걸리면 중간에 한 번 더 `git pull origin main` 하십시오.
> 이 저장소는 하루에도 여러 번 갱신됩니다.

### 그리고 **이슈부터 만듭니다.**

코드나 문서를 건드리기 **전에** 이슈를 엽니다. 순서가 반대면 안 됩니다.

```
이슈 -> 브랜치 -> 작업 -> PR (Closes #번호) -> 머지
```

**왜.** 대화에만 있는 일은 없는 일입니다. 2026-08-26 에 일정 시스템을 설계해 놓고
이슈로 안 남겨 **6개 항목이 하나도 구현되지 않은 채 통째로 사라졌습니다.**
8/28 에 저장소와 대조해서야 알았습니다.

| 이럴 때 | 이슈를 |
|---|---|
| 코드·문서를 고친다 | **먼저 연다** |
| 회의에서 「이거 하자」가 나왔다 | **그 자리에서 연다.** 나중에 정리하지 않는다 |
| 규칙을 새로 정한다 | 연다. 규칙 제정도 작업이다 |
| 남이 시킨 것을 한다 | 연다. 시킨 사람이 안 열었으면 내가 연다 |
| 오타 하나 고친다 | 안 열어도 된다 |

브랜치 이름은 `feature/작업명-이름` 입니다. 이슈 번호는 브랜치가 아니라
**커밋·PR 의 `Closes #번호`** 가 잇습니다.

---

## 1. 이 저장소가 무엇인가

FOOTHOLD 팀(인공지능사관학교 7기)의 **문서·연구 허브**입니다.
Unitree Go2 사족보행 로봇의 미경험 험지 적응을 다룹니다.

**소프트웨어 제품이 아닙니다.** 애플리케이션 서버 · 데이터베이스 · 빌드 시스템 ·
테스트 스위트 · 의존성 매니페스트가 없습니다. 그런 것을 만들려 하지 마십시오.

- 실행되는 RL · 시뮬 코드는 **다른 저장소**에 있고 GPU 와 Isaac Sim 이 필요합니다.
- 여기서 돌릴 수 있는 것은 **Python 3 표준 라이브러리**뿐입니다.
  `requirements.txt` · `package.json` · 락파일이 없습니다. **설치할 것이 없습니다.**

```
docs/           공개 웹으로 나가는 정본. 팀장이 관리한다
  meetings/     회의록
  research/     연구·실험·계획 문서
inbox/          팀원 제출함. 자기 폴더에만 쓴다
  jay/ lee/ lim/ meang/ oh/
.github/        자동화 (워크플로 16종 + 검수 스크립트)
tools/          분석 스크립트
```

---

## 2. 어디에 쓰는가 (가장 자주 어기는 규칙)

| 무엇을 | 어디에 | 비고 |
|---|---|---|
| 팀원이 만든 모든 것 | **`inbox/<자기이름>/`** | 형식 자유. 다듬지 않아도 됩니다 |
| 정리된 정본 | `docs/` | **팀장이 승격합니다. 직접 쓰지 마십시오** |
| 자동화 | `.github/` | 팀장 담당 |

**`docs/` 를 직접 고치지 마십시오.** 이미 정리된 정본이라, 모르고 덮으면
남이 몇 시간 들여 쓴 것이 사라집니다. 실제로 그렇게 회의록 정본이 팀원 노트 판으로
덮인 적이 있습니다(2026-08-25).

고칠 것이 보이면 **이슈로 알리거나, `inbox/` 에 수정안을 올리십시오.**

**`docs/` 나 루트 파일을 건드리는 PR 은 팀장 검토 후에만 머지됩니다.**
`inbox/` 아래만 있는 PR 도 **팀장이 확인하고 머지합니다.** 내용은 손대지 않습니다 (2026-08-27 개정).

---

### 이슈를 «만드는 법» (2026-09-01 확정 · 세 번 어겨서 박아 둡니다)

폼(`gh issue create` 웹 화면)으로 만들면 저절로 되지만, **CLI 나 API 로 만들면
분류가 통째로 빠집니다.** 분류 봇은 라벨이 아니라 **본문의 절**을 읽습니다.

```
### 작업 영역

C/운영 · 인프라 · 자동화 · 깃 · 웹 · 예산 · 일정

### 어느 관문까지

A-정책 · 9월 12일

---

(여기부터 본문)
```

**이 두 절이 없으면** 봇이 「★ 분류 안 됨. 폼에서 «작업 영역»을 안 골랐습니다」를
팀장에게 보냅니다. 라벨을 손으로 붙여도 소용없습니다. 봇은 본문만 봅니다.

**작업 영역** (`docs/ROLES.md` 8갈래 · 문자열을 그대로 씁니다)

```
A/정책학습 · 학습 · 보상 · 관측 · 파인튜닝
A/지형씬제작 · 지형과 씬 에셋 제작
A/평가 · 일반화 벤치마크 · 지표
A/트윈렌더 · 실공간 복원 · 3DGS · 렌더 · 합성데이터
B/항법 · ROS2 · TF · SLAM · Nav2
B/인지 · LiDAR · 카메라 · 센서 융합
C/기록 · 회의록 · 문서 · 백과 · 커리큘럼 · 발표
C/운영 · 인프라 · 자동화 · 깃 · 웹 · 예산 · 일정
```

**어느 관문까지**

```
A-정책 · 9월 12일
MVP · 중간발표 · 9월 30일
NAV · 실기 항법 · 11월 7일
FINAL · 최종발표 · 12월 11일
```

기획발표(9/4)는 폼 선택지에 없습니다. 마일스톤만 `gh issue edit --milestone 기획발표`
로 붙이고 이 절은 비웁니다.

**제목은 명사형으로.** 「…한다」로 끝내지 않습니다.

- 나쁨 `[작업] 평지 직진 기준선을 세우고 10종을 재측정한다`
- 좋음 `[작업] 평지 직진 기준선 수립 + 미경험 10종 동일 조건 재측정`

보드에서 제목만 훑을 때 「무슨 작업인지」가 한눈에 들어와야 팀원이 집어 갑니다.

**만든 뒤 되읽어 확인합니다.**

```bash
gh issue view <번호> --json labels,milestone,body \
  --jq '(.labels|map(.name)|join(",")) + " | " + (.milestone.title // "없음")'
gh issue view <번호> --json body --jq '.body' | grep -c "^### 작업 영역"
```

마지막 줄이 `1` 이 아니면 분류가 안 된 것입니다.

> **왜 이 절이 여기 있나.** 2026-09-01 에 CLI 로 이슈 8건을 만들면서 라벨과
> 마일스톤만 붙이고 본문 절을 빼먹었습니다. 봇이 매번 「분류 안 됨」을 팀장에게
> 보냈고, 팀장이 세 번 지적했습니다. 규칙을 기억에 두지 않고 여기 박습니다.

## 3. 브랜치와 PR

```bash
git checkout -b feature/작업명-이름     # 예: feature/terrain-scene-meang
git add inbox/내이름/파일.md
git commit -m "작업 내용"
git push -u origin feature/작업명-이름
```

- **브랜치 이름에 이슈 번호를 붙이지 않습니다.** `feature/작업명-이름` 형식입니다.
- **`main` 에 직접 push 하지 마십시오.** 감시 워크플로가 알립니다.
- PR 본문에 **`Closes #번호`** 를 쓰면 머지될 때 이슈가 자동으로 닫힙니다.
  「했다」와 「실제로 머지됐다」가 같아집니다. 되도록 쓰십시오.

---

## 4. 문서를 쓸 때

### 4-1. 머리 여섯 줄

`docs/` 로 승격될 문서는 맨 위에 이것이 있어야 합니다.
**`inbox/` 에 올릴 때는 없어도 되지만, 달아 두면 그대로 승격됩니다.**

```
> 분류: 리서치
> 작성: 홍길동 · 2026-08-27 09:41
> 근거: 공식 문서
> 요지: 한 줄로 이 문서가 말하는 것
> 상태: 확정
> 판: v1.0
```

**「판」은 2026-08-28 에 늘어난 여섯째 줄입니다.** 살아 있는 문서(규칙·계획·실측·조사)에만 답니다.
회의록·다이제스트 같은 **기록물은 고치지 않으므로 판이 없습니다.**

판을 달면 문서 끝에 이력표도 답니다. 이것이 프로젝트가 어떻게 발전했는지의 기록이 됩니다.

```
## 4-1. 웹에 무엇을 만들 때 (반복 지적 · 또 어기면 다시 만든다)

팀장이 여러 번 지적한 것들이다. 매번 같은 자리에서 어겼으므로 여기 적는다.

### 본문 폭을 줄이지 않는다

**`max-width` 를 문단에 걸지 않는다.** 특히 섹션 제목 바로 아래 문단이 그렇다. `max-width: 64ch` 같은 것을 넣으면 제목은 넓고 본문만 좁아져, 읽는 사람이 「왜 여기만 들여썼지」를 묻게 된다.

폭은 바깥 그릇(`.wrap`)이 한 번만 정한다. 안쪽 문단은 그 폭을 그대로 쓴다.

```css
/* 이렇게 */          .wrap{max-width:960px}   p{}
/* 이렇게 말고 */     .wrap{max-width:960px}   p{max-width:68ch}
```

### 옅은 회색 글씨를 쓰지 않는다

**본문과 작은 라벨은 배경 대비 4.5:1 이상.** 차트 축 라벨·범례·캡션이 자주 걸린다. `#C6C5BF` `#B0AFA9` 같은 값은 흰 바탕에서 안 읽힌다.

차트 안 글씨도 같다. **안 보이면 없는 것과 같다.**

### 색은 우리 색 체계에서 고른다

`lieflat-charts` 의 `color-presets.js` 에서 한 벌을 고른다. 회색 계조(Mono)는 «고를 것이 없을 때»의 보루이지 기본값이 아니다.

| 자료 모양 | 고를 것 |
|---|---|
| 순서 있는 값 하나 (성공률·진도·열지도) | `porcelain` · 색상 하나에 명도로 |
| 순서 없는 갈래 4개 이하 | `palm` |
| 절제하되 주인공 하나 | `wire` |
| 갈래가 7개 이상 | 그때 Mono |

**한 산출물에 한 체계만.** 섞지 않는다.

### 그 밖

- **em dash 금지.** 웹·이슈·PR·커밋·채팅 전부. 가운뎃점이나 마침표를 쓴다
- **웹에 ASCII 아트 금지.** 구조도는 SVG 나 표로
- **틀에 박힌 배치 금지.** 같은 크기 카드의 반복, 뜻 없는 번호 매기기

---

## 4-2. 공식 문서를 내기 전에 반드시 (검증 없이 발행 금지)

보고서 · 정본 · 웹 게시물 · 팀 공유물을 내기 전에 **반드시** `codex exec --model gpt-6-astra -c model_reasoning_effort=high` 로 검증받는다. 「다 됐다」 다음이 아니라 **발행 «전»** 이다.

### 무엇을 보게 하나

| 볼 것 | 물을 것 |
|---|---|
| **한국어 표현** | 어색하거나 번역투인 문장이 있는가. 격식에 안 맞는 말투가 있는가 |
| **근거** | 모든 수치가 원자료에서 나왔는가. 손으로 적은 숫자가 있는가 |
| **오해** | 읽는 사람이 본문과 다르게 이해할 자리가 있는가 |
| **과장** | 자료가 말하는 것보다 세게 말한 자리가 있는가 |

검증자가 우리 주장을 **다시 재도록** 시킨다. 「우리가 잰 값이 이렇다」를 믿게 하지 말고 자기가 세어 틀리면 틀렸다고 말하게 한다.

### 왜 이 조항이 있나

2026-09-12 에 종합보고서 1차 초안에 「고속에서 기준선이 무너진다」고 썼다. 축별로 다시 세니 **14종 중 8종은 넘어지지 않고 그냥 더 느리게 걷는 것**이었다. 「무너진다」는 과장이었고, 영상 캡션에 「성공률 0 %」만 적혀 있어 멀쩡히 걷는 로봇을 보면서 넘어진 줄 알게 돼 있었다.

팀장이 「0 %가 맞나」고 물어 잡혔다. **검증을 먼저 걸었으면 팀장이 아니라 검증자가 잡았을 것이다.**

### 한 번으로 안 끝난다 (2026-09-12 확정)

**검증은 통과할 때까지 돌린다.** 한 번 받고 고친 뒤 발행하면 안 된다.

| 차수 | 막은 것 | 그중 앞 차수의 미해결 |
|---|--:|--:|
| 1회차 | 7건 | - |
| 2회차 | 8건 | **4건** |
| 3회차 | 발행 판정 대기 | - |

2회차가 막은 여덟 건 가운데 **넷이 1회차에서 이미 지적받은 것**이었다. 고쳤다고
믿은 것과 실제로 고쳐진 것이 달랐다. 대표적으로 「고속에서 기준선이 무너진다」를
축별 표로 바꿔 놓고, **같은 엄밀함을 rails 설명에는 안 썼다.** rails 실패 52판 중
27판은 방향 축만 떨어졌는데 본문은 「턱을 타고 절반이 걸린다」고 적고 있었다.

그래서 두 가지를 더 건다.

1. **고쳤다는 주장도 검증 대상으로 넘긴다.** 의뢰서에 「아래 조치도 직접 재서
   확인하라」를 적는다. 안 적으면 검증자가 우리 말을 믿는다.
2. **관문을 코드로 남긴다.** `sim/eval/report/audit.py` 가 지난 차수에 막힌
   것들을 스스로 센다. 사람이 훑으면 또 빠진다.

### 어떻게 거는가

```bash
codex exec --model gpt-6-astra -c model_reasoning_effort=high \
  -s danger-full-access --skip-git-repo-check \
  "<의뢰서 파일>을 읽고 전부 수행하라. 의뢰서의 주장을 근거로 삼지 말고
   인용된 수치도 직접 다시 재라. 한국어 표현 · 근거 · 오해 소지 · 과장을 본다."
```

검증 결과에서 나온 지적은 **고치고 다시 검증**한다. 한 번으로 끝내지 않는다.

---

## 판 이력

| 판 | 언제 | 무엇이 바뀌었나 | 근거 |
|---|---|---|---|
| **v2.0** | 2026-08-28 | 관문 4개 -> 5개 | #76 |
| v1.0 | 2026-08-09 | 처음 씀 | 이전 이력은 git 에 |
```

**올리는 기준**: major(v1 -> v2) 결정이 바뀌어 기존 서술이 뒤집힘 · minor(v1.0 -> v1.1) 보강·추가 · 오타와 서식은 올리지 않습니다.

**회의록에는 한 줄이 더 붙습니다.** 어느 자리의 회의인지 밝힙니다.

```
> 자리: 팀내부 | 멘토링 | 운영진 | 조선대
```

회의 목록이 자리별로 갈려서, 멘토링 기록과 팀 내부 결정이 섞이지 않습니다.

**분류는 여덟 중 하나입니다.**

```
회의록 · 리서치 · 실험 · 계획 · 결정 · 운영 · 현장 · 가이드
```

**내용과 분류가 맞아야 합니다.** 본인 학습 계획을 「회의록」으로 올리면 회의가 아닌 것이
회의 목록에 섞입니다. 그건 「계획」입니다. 실제로 그런 일이 있었습니다.

`작성` 은 **`이름 · YYYY-MM-DD HH:MM`** 형식입니다. **시각까지 필수입니다**
(2026-08-28 부터. 그 전에는 날짜만 써도 통과해서 27개 중 4개만 시각이 있었습니다).
「언제 쓴 글인지」가 분 단위로 남아야 같은 날 여러 번 고친 순서가 보입니다.
다른 형식은 **웹 배포 때** 빌드가 막습니다. PR 은 막지 않고 알려만 줍니다.

### 4-2. 근거를 표기합니다

주장에는 신뢰도를 붙입니다. 이게 이 팀 문서의 핵심 규칙입니다.

```
`확인됨`   직접 보거나 재서 확인한 것
`추측`     그럴듯하지만 확인 안 한 것
`미확인`   확인이 필요한 것
`미측정`   재보지 않은 수치
```

**재보지 않은 것에 「확인했다」를 쓰지 마십시오.** 확인 비용이 2분이면 확인하십시오.

숫자를 말할 때는 **어디서 나온 숫자인지** 함께 적습니다.
링크가 있으면 링크를, 실측이면 실측 조건을.

### 4-3. 문체

- **em dash 를 쓰지 마십시오.** 가로로 긴 줄표(유니코드 U+2014)입니다. **웹 배포 때** 빌드가 막습니다. PR 은 막지 않고 알려만 줍니다. `·` 나 마침표를 쓰십시오.
- **선으로 그린 글자 도식을 쓰지 마십시오.** 표로 바꾸십시오. 이것도 **웹 배포 때** 걸립니다.
- 문서 안에서 새 용어를 만들지 마십시오. 공식 프로젝트 페이지입니다.

---

### 4-4. AI 슬롭을 만들지 마십시오

기계가 찍어낸 티가 나는 산출물을 올리지 않습니다. 셋입니다.

**① 웹에 ASCII 아트 금지.** 구조도·흐름도는 SVG 나 표로 만듭니다.
`┌─┐` `│` `→` 같은 상자 문자로 그린 그림은 화면 폭에 따라 깨지고, 화면 읽기
프로그램이 읽지 못합니다. **md 원문에는 무방합니다.** 웹으로 나갈 때가 문제입니다.
빌드 `[1.82]` 가 막습니다.

**② em dash(`·`) 금지.** 웹만이 아니라 **이슈 제목·본문·커밋 메시지·PR·채팅 전부**입니다.
`·` 나 마침표를 씁니다. 빌드는 웹만 보므로 나머지는 **본인이 지켜야 합니다.**
2026-08-28 실측에서 이슈 10개가 오염돼 있었습니다.

**③ 틀에 박힌 배치 금지.** 세로로 길게 나열하기, 뜻 없는 번호 매기기,
층위가 섞인 묶음 이름(「문서」 같은 것), 같은 크기 카드의 반복.
**크기와 순서가 뜻을 실어야 합니다.** 중요한 것이 크고 먼저 와야 합니다.

> 판정 기준: **「이걸 사람이 골라서 이렇게 놓았는가, 아니면 그냥 나온 대로인가」.**
> 후자면 슬롭입니다.

## 5. 절대 하면 안 되는 것

| 금지 | 왜 |
|---|---|
| 비밀번호 · API 키 · 토큰 · 개인 연락처를 저장소에 적기 | 공개 웹으로 나갑니다. 되돌려도 git 이력에 남습니다 |
| **조선대 강의 자료 원본을 저장소에 넣기** | 조선대 저작물입니다. 자료실(암호)에만 둡니다 |
| **예산 금액을 공개 문서에 적기** | 대외비입니다. 내부 예산 문서에만 |
| 개인 사정을 팀 이슈로 올리기 | 팀 저장소는 팀 일만 다룹니다 |
| 100MB 넘는 파일을 git 에 넣기 | GitHub 이 거부합니다. NAS 나 클라우드에 두고 링크만 남기십시오 |
| `main` 에 force push | 남의 작업이 사라집니다 |

**AI 에게 파일을 통째로 넣을 때 주의하십시오.** 조선대 자료 원본 · 팀 내부 문서 ·
계정 정보 · 개인정보는 넣지 마십시오.

---

## 6. 자동 검수가 무엇을 보는가

`inbox/` 에 PR 을 올리면 `.github/scripts/review_inbox.py` 가 자동으로 돌고
결과가 팀장 텔레그램으로 갑니다. **보는 항목은 이렇습니다.**

| 항목 | 통과 조건 |
|---|---|
| h1 제목 | 맨 위 `# 제목` 한 줄 |
| 메타데이터 5줄 | 분류 · 작성 · 근거 · 요지 |
| 분류가 정해진 값 | 위 여덟 중 하나 |
| 작성자·날짜 형식 | `이름 · YYYY-MM-DD` |
| 증거 표기 | `확인됨` 등이 하나라도 |
| 원 출처 링크 | 참고한 곳 |
| 웹 금지 요소 없음 | em dash 0 · 글자 도식 없음 |

직접 돌려볼 수 있습니다.

```bash
python3 .github/scripts/review_inbox.py inbox/내이름/*.md
```

**「손질 필요」가 나와도 제출은 됩니다.** 승격 전에 채우면 됩니다.

---

## 7. 이 환경에서 안 되는 것

- `tools/robogauge/*.py` : `torch` · `numpy` 가 필요하고 Windows 절대경로가
  박혀 있습니다. 여기서 안 돌아갑니다. **읽기용입니다.**
- 실기 로봇 · Isaac Sim 관련 코드 : GPU 와 라이선스가 필요합니다.
- 자료실 암호화 : 비밀번호가 팀장 로컬에만 있습니다.

---

- **이슈 템플릿의 `name:` 을 한 단어로 짓지 마십시오.** `작업`·`실험` 같은 한 단어
  이름은 GitHub 이 템플릿 목록에서 오류 없이 조용히 숨깁니다 (2026-08-27, lab·go2
  양쪽에서 재현 6회). 두 단어 이상으로 짓고, 바꾼 뒤에는 반드시
  `issues/new/choose` 화면을 열어 눈으로 확인하십시오.

## 8. 팀 규칙 원문

이 파일은 요약입니다. 원문은 이쪽입니다.

- 협업 규칙 `docs/COLLAB.md`
- 제출 안내 `inbox/README.md`
- 자동화 지도 https://foothold-project.vercel.app/automation
- 프로젝트 페이지 https://foothold-project.vercel.app

---
