# 설계 세션 기록: 2026-08-26 저녁 ~ 08-27 오전

> 분류: 결정
> 작성: 오흥재 · 2026-08-27 10:05
> 근거: 본인 노트
> 요지: 8/26 18시부터 8/27 오전까지 팀장이 확정한 설계 결정 전부. 대화가 아니라 파일로 남긴다.
> 상태: 확정

> **왜 이 문서가 있나.** 이 세션의 대화는 길어서 중간에 한 번 압축됐고, 그 과정에서
> 초반에 확정한 것 몇 가지가 실제로 잃어버려 다시 물어야 했다. 실제로 벌어진 일이다.
>
> **철칙 2 · 모든 데이터는 휘발되지 않는다.** 기억이 아니라 시스템·구조로 남긴다.
> 세션 안에서만 한 설명은 세션이 끝나면 사라진다. 그래서 여기 박는다.
>
> 이 문서는 **결정만** 담는다. 왜 그렇게 정했는지의 근거는 각 항목에 한 줄로 붙인다.

---

## 1. 이슈와 분류

| # | 결정 | 근거 |
|---|---|---|
| 1-1 | 이슈 템플릿 `name` 을 **한 단어로 짓지 않는다** | `작업`·`실험` 은 GitHub 이 목록에서 오류 없이 숨긴다. 재현 6회. 원인 기전은 `미확인` |
| 1-2 | 템플릿 이름: **작업 등록 · 실험 기록 · 리서치 · 막힘·오류** | 「작업 선언」은 하는 일과 어긋난다. 팀장 표현대로 「미리 등록」하는 곳 |
| 1-3 | 폼 문구를 **「작업 영역」** 으로 (「어느 갈래인가」 폐기) | 공식 프로젝트 문서에 쓸 말이 아니다 |
| 1-4 | **작업 영역 8갈래**. 트랙 접두어를 붙인다 | 옛 5갈래는 8/19 두 트랙 전환 이전 것이라 「실기 전이」가 폐기된 목표 이름이었고 트랙 B 가 통째로 빠져 있었다 |
| 1-5 | 색은 **트랙**으로 묶는다. A 초록 · B 파랑 · 공통 회색 | 보드에서 색만 보고 트랙을 안다 |
| 1-6 | **마일스톤 5개**로 재정의 | 아래 표 |
| 1-7 | 폼의 답을 **라벨·마일스톤으로 자동 이관** | 폼은 이미 묻고 있었는데 답이 본문 글자로만 남아 보드에서 묶이지 않았다. 팀원이 안 적어서가 아니라 적은 것이 옮겨지지 않아서였다 |
| 1-8 | **이슈는 만든 사람이 닫는다** | 승격 검토(`[승격 검토]`)만 봇이 만들고 팀장이 닫는다 |
| 1-9 | 팀원 계획을 **팀장이 대신 이슈로 발행하지 않는다** | 본인이 만들지 않은 이슈는 본인이 닫지 않아 보드에 남는다 |
| 1-10 | **이슈를 만드는 것이 곧 계획이다.** 개인 계획 문서를 따로 쓰지 않는다 | 두 곳에 적으면 한쪽만 고쳐진다 (`DECISIONS 08-13`). 실제로 라현 계획이 문서엔 있고 일정엔 없었다 |

### 작업 영역 8갈래

| 코드 | 라벨 | Main | 폴더 |
|---|---|---|---|
| A1 | `A/정책학습` | 임석헌 | `sim/policy/` |
| A2 | `A/지형씬제작` | 맹라현 | `sim/terrain/` |
| A3 | `A/평가` | 오현민 | `sim/eval/` |
| A4 | `A/트윈렌더` | **맹라현** | `sim/twin/` |
| B1 | `B/항법` | 오흥재 | `nav/` |
| B2 | `B/인지` | 오현민 | `nav/perception/` |
| C1 | `C/기록` | 이민우 | `docs/` |
| C2 | `C/운영` | 오흥재 | `.github/` |

> A4 담당은 한 번 오흥재로 바꿨다가 **팀장 지시로 맹라현으로 되돌렸다** (8/27).

### 마일스톤 5개

| 마일스톤 | 기한 | 끝났다는 뜻 | 근거 |
|---|---|---|---|
| `A-정책` | 9/12 | 우리 지형으로 학습한 정책과 지표 세트 | REPORT §3 ②③④ · 렌더 실측 역산 |
| `MVP · 중간발표` | 9/30 | **트랙 A 완결.** 일반화 평가표 + 트윈·렌더 시연물 | 팀장 확정. 「10월 초」 언급이 있었으나 9/30 기준으로 진행 |
| `NAV · 실기 항법` | 11/7 | 조선대 Go2 에서 SLAM 지도 + Nav2 경로가 한 번 돈다 | 자체 대여 시작(11/11) 직전에 조선대 단계를 닫는다 |
| `FINAL · 최종발표` | 12/11 | 지정 코스 자율주행 시연 | 팀장 확정 |

**히든 트랙**: 트랙 A 가 중간발표에서 끝나지 않고 최종까지 더 발전할 수 있다.
약속이 아니라 여지이므로 마일스톤에 넣지 않는다. 살아나면 이슈에 `히든` 라벨로 단다.

---

## 2. 역할

8/21 팀 미팅 표가 원본이고, 아래는 **2026-08-27 팀장이 확장한 것**이다.

| 영역 | Main | Sub |
|---|---|---|
| RL 정책·학습 | 임석헌 | 이민우 · 오현민 · 맹라현 · 오흥재 |
| 디지털 트윈·씬 | 맹라현 | 오흥재 · 이민우 |
| CV·인지·평가 | 오현민 | 맹라현 · **이민우** |
| 항법 (SLAM·Nav2) | 오흥재 | 임석헌 · 이민우 · 오현민 · 맹라현 |

- 팀장은 파트 배정과 별개로 전 영역에 관여한다. 표는 **파트를 끄는 사람**을 적은 것이다.
- 「전원」이라고 쓰지 않고 **이름으로 푼다.** 「전원」은 아무도 아니라는 뜻으로 읽힌다.
- `공통/기록` main 은 **이민우**다. 맹라현은 이미 두 갈래 main 이라 과부하이고,
  이민우가 하던 「운영진 대상 문서화」가 이 갈래와 같은 일이다.

### 실패 지형 5종

**5인이 하나씩 맡아 병렬로 진행한다. 개인 배정은 팀 회의에서 정한다** `미확정` (이슈 `#59`).

| 지형 | 실패 모드 | 무엇이 문제인가 |
|---|---|---|
| `stepping_stones` `pit` `floating_ring` | 전진 불능 | 관측이 위험(구멍·레일)을 실제로 보는가 |
| `gap` `rails` | 낙상 | 발 배치와 접촉 페널티 |

원인이 다르므로 처방도 달라야 하고, 한 사람이 순차로 다루면 시행착오가 직렬로 쌓인다.
**병렬로 나누는 것은 학습이 아니라 「레시피 찾기」다.** 최종 학습은 혼합 단일 실행 하나다.

---

## 3. 저장소와 폴더

| # | 결정 | 근거 |
|---|---|---|
| 3-1 | `foothold-rl` → **`foothold-go2`** | RL 이라는 이름이 트랙 B(ROS2·SLAM·Nav2)를 담을 수 없다 |
| 3-2 | 폴더는 **`sim/` `nav/` `common/`**. 트랙 이름을 폴더에 박지 않는다 | 히든 트랙이 현실이 되면 `track-a-` 가 거짓말이 된다 |
| 3-3 | **lab 과 go2 가 같은 트리**를 쓴다 | lab 은 문서 창고가 아니라 개발 공간이기도 하다. 경로가 같으면 옮길 때 판단이 없다 |
| 3-4 | go2 의 문서 폴더는 **`guide/`(재현 절차) · `report/`(발표 후 공개본)** | `docs/` 를 쓰면 lab 의 `docs/`(정본)와 뜻이 겹쳐 사고가 난다 |
| 3-5 | **코드는 inbox 를 거치지 않는다.** 브랜치 → PR → `sim/`·`nav/` 직접 | 문서는 팀장이 옮겨도 뜻이 안 변하지만 코드는 옮기면 도는지 보장할 수 없다 |
| 3-6 | inbox 는 **자유**다. 구조를 강요하지 않는다 | `inbox/<이름>/` 아래는 구조적으로 충돌이 불가능하다. 그게 inbox 의 값이다 |
| 3-7 | `inbox/jay/` 신설 | 팀장 제출함이 없었다 |

### 브랜치

```
feature/주제-이름      새로 만드는 것
fix/무엇이안되나-이름    고치는 것
docs/문서명-이름        문서만
```

- **이슈 번호를 붙이지 않는다.** 뒤에 자기 이름을 붙인다.
- 추적은 커밋·PR 의 **`Closes #번호`** 가 한다.
- `foothold-go2` 는 한 단계 더: `feature/…` → PR → `dev` → 검증된 것만 → `main`

| 이름 | 임석헌 | 맹라현 | 오현민 | 오흥재 | 이민우 |
|---|---|---|---|---|---|
| 브랜치 약자 | `lim` | `meang` | `oh` | `jay` | `lee` |
| GitHub 핸들 | `IQ152` | `maengu86` | `less82` | `vfxpedia` | `lmw0207` |

### 작업 흐름 (정본은 `docs/WORKFLOW.md`)

| 순서 | 무엇 | 보드 |
|---|---|---|
| 1 | 이슈를 만든다. 본인 assign · 기간 | Todo (자동) |
| 2 | 폼에서 작업 영역·관문을 고른다 | 라벨·마일스톤 (자동) |
| 3 | 브랜치를 판다 | |
| 4 | **이슈를 In Progress 로 옮긴다** | 손으로. 이것 하나뿐 |
| 5 | 작업·커밋 | |
| 6 | PR 을 연다 (`Closes #29`) | In Progress (자동) |
| 7 | 팀장 검토 | |
| 8 | 머지 → 이슈 자동 닫힘 | Done (자동) |

**`Closes #29` 는 PR 을 열 때가 아니라 머지될 때 닫는다.**
그래서 PR 이 먼저다. `main` 에 안 들어간 작업은 아직 끝난 게 아니다.

---

## 4. 문서

| # | 결정 | 근거 |
|---|---|---|
| 4-1 | 머리 **다섯 줄**: 분류 · 작성 · 근거 · 요지 · 상태 | 네 줄이라 적힌 곳이 여러 곳이었다. 다섯으로 통일 |
| 4-2 | 작성은 **`이름 · YYYY-MM-DD HH:MM`** | 날짜만도 통과하나 시각까지 쓰면 같은 날 수정 순서가 보인다 |
| 4-3 | **파일 100MB 미만이면 git 에 올려도 된다** | GitHub 이 거부하는 실제 경계가 100MB 다. 내가 임의로 10MB 로 좁혔던 것을 되돌렸다 |
| 4-4 | 회의록에 **`> 자리:`** 한 줄 추가 (팀내부·멘토링·운영진·조선대) | 멘토 조언과 팀이 스스로 정한 결정이 같은 줄에 있으면 「누가 정한 것인가」를 매번 되짚어야 한다 |
| 4-5 | **덧붙인 것은 덧붙였다고 날짜와 함께 표시한다** | 「증발 금지」를 지켜도 뜻은 바뀔 수 있다. 라현이 회의록에 해설 한 문단을 덧붙였는데 그것이 원래 표를 「뒤집힌 결정」처럼 읽히게 했다 |
| 4-6 | `AGENTS.md` 에 **공통 규칙만**. 개인 설정은 `inbox/<이름>/AGENTS.md` 또는 로컬 | 에이전트마다 도구가 달라, 공통 파일에 개인 설정이 섞이면 남의 에이전트가 그것을 규칙으로 읽는다 |
| 4-7 | em dash 전면 금지 | `os/` 130 · lab 38 · go2 7 전부 제거. 다만 **`·` 는 단어 단위 「그리고」라** 절 단위 dash 를 기계적으로 바꾸면 의미가 붕괴한다. 실제로 커널 6행이 깨졌다 |
| 4-8 | 산출물 **상태는 문서의 `> 상태:` 줄이 정한다.** 하드코딩하지 않는다 | 문서를 고치면 다음 배포에 그대로 반영된다 |
| 4-9 | **문서만 남기는 것으로 끝내지 않는다. 웹에 반영한다** | md 는 원고, 사람이 보는 것은 웹, 제출하는 것은 그 웹을 뽑은 PDF |

---

## 5. 산출물 (9월 4일 제출)

운영진 산출문서 안내에는 열한 개가 있지만 **실증 프로젝트가 내는 것은 셋**이다.

| 공식 번호 | 문서 | 우리 파일 |
|---|---|---|
| 1.1 | 브레인 스토밍 | `deliverables/plan/brainstorming.md` |
| 1.2.1 | 프로젝트 기획서 (요약) | `deliverables/plan/proposal-summary.md` |
| 1.2.2 | 프로젝트 기획서 (기획발표용) | `deliverables/plan/proposal-deck.md` |
| 3.1 | WBS | `deliverables/plan/wbs.md` → `wbs.xlsx` |

**내지 않는 것**: 1.3 요구사항 정의서 · 2.x 설계 3종 (팀장 확정).
**제출은 아니나 계속 쌓는 것**: 3.2 진행일지 · 3.3 멘토링일지.

- 폴더는 **발표 시점 기준** (`plan/` `midterm/` `final/` `logs/`).
  공식 예시 번호(01·02·03·04)를 따라가면 02 가 비어 어색하다.
- 공식 폴더명 변환은 `make_zip.py` 의 SUBMIT 표가 **파일 단위로** 한다.
- 제출본에서 **내부 메타(분류·상태·완료 기준)를 뗀다.** 채점받는 문서에 「상태: 초안」은 해롭다.
- 트랙 공식 명칭은 **`AI피지컬`**.

### WBS

| 층 | 무엇 | 누가 정하나 |
|---|---|---|
| 1 | 작업 영역 8개 | 사람 (고정) |
| 2 | 관문별 산출물 | 사람 (관문마다) |
| 3 | 세부 작업 | **이슈에서 자동** |

1·2층이 「WBS 계획서」이고 이슈가 하나도 없어도 그려진다.
엑셀은 **주간 다이제스트에 편승** + Actions 수동 버튼. 즉시 반영이 아니다.

**이슈 제목에 동사 규칙을 강제하지 않는다** (팀장 철회). `gap 지형 파인튜닝` 으로 충분하다.

---

## 6. 웹

| # | 결정 | 근거 |
|---|---|---|
| 6-1 | 표지 D-day: **MVP 는 D-N, FINAL 은 초 단위.** 제출 서류를 같이 표시 | 팀장 확정 문구: 「MVP D-35, FINAL 107일 00:00:00 이런식으로」 |
| 6-2 | 그보다 임박한 관문이 있으면 **맨 앞에 하나 더** | 급한 것을 표지에서 빼지 않는다 |
| 6-3 | D-day 띠는 **다이제스트 띠보다 위** | 「언제까지」가 「무엇을 했나」보다 급하다 |
| 6-4 | 허브 카드에 **넘버링 · 작성자 · 작성일** | 「이 글이 언제 등록되었는지 메타 정보도 없어 UX 적으로 좋지 않다」 |
| 6-5 | 회의 허브를 **자리별 섹션**으로 | 위 4-4 |
| 6-6 | 산출물 문서를 **웹 페이지로 게시**하고 현황 표에서 링크 | 위 4-9 |
| 6-7 | 홈페이지 구조화·네비·경로 정리는 **맨 마지막에 한 번에** | 폴더 정리 전에 하면 두 번 고친다 |
| 6-8 | 브랜드 정본은 **`foothold-brand` 저장소**. Figma 는 거울 | Figma 파일 자체가 「Brand 1.1.0 · Git is canonical」이라고 적고 있다 |
| 6-9 | 발표 덱은 **`team-intro` 완성도 + `pitch#12` 엔딩 + brand 승인본** | `pitch` 판의 발자국 점선 도식은 팀장 반려 |

---

## 7. 자동화

| 무엇 | 언제 도나 | 확인 |
|---|---|---|
| 이슈 자동 분류 (`issue-triage.yml`) | 이슈 열림·수정 | 시험 이슈 `#57`·`#59` |
| PR 봇 머리 다섯 줄 코멘트 | inbox PR | 시험 PR `#58`·`#60` |
| WBS 엑셀 생성 | 주간 리포트에 편승 + 수동 | 실행 확인 |
| 다이제스트 **재료** 수집 | 일요일 21시 KST | 실행 확인 |
| 승격 검토 이슈 | **PR 머지 뒤** (원래 그랬음) | 트리거 확인 |

**다이제스트는 재료만 자동으로 모은다.** 「무엇을 알게 됐나」는 판단이라 기계가 못 쓴다.
커밋 목록에서 「보상이 높다고 걷는 것이 아니다」 같은 문장은 나오지 않는다.

---

## 8. 이 세션에서 배운 것 (재발 방지)

| 무엇 | 어떻게 막나 |
|---|---|
| **초록불이 거짓말을 할 수 있다** | 폐기된 어휘로 매칭하던 워크플로가 `exit 0` 으로 계속 success 를 냈다. 빈 산출물이 성공이 되는 경로를 남기지 않는다 |
| **내 검사식이 내 코드에 걸린다** | `id: quest` 가 `id: question` 에, `. ./triage.env` 가 내가 쓴 주석에 걸렸다. 줄 전체 일치로 본다 |
| **UTC 로 날짜를 찍으면 한국 새벽에 어제가 된다** | GitHub Actions 러너에서는 `TZ=Asia/Seoul` 을 쓴다. **단 이 워크스테이션의 Git Bash 에서는 쓰지 않는다** (시계가 이미 KST 라 9시간 어긋난다. 2026-08-27 실측: 11:43 -> 02:43). 로컬에서는 `date` 를 그대로 쓰거나 PowerShell `(Get-Date)` 를 쓴다 |
| **heredoc 안 파이썬의 백슬래시가 먹힌다** | 파이썬 코드는 파일 도구로 쓴다. `chr(10)`·`chr(92)` 로 우회 |
| **대화가 압축되면 결정이 사라진다** | 이 문서가 그 대응이다 |

---

## 9. 아직 안 정해진 것

> **닫힌 것 (2026-08-27)**: 실패 5종 개인 배정. `gap` 임석헌 · `rails` 맹라현 ·
> `stepping_stones` 오현민 · `pit` 이민우 · `floating_ring` 오흥재.
> 근거와 각자 만질 파라미터는 [실패 지형 파인튜닝 실행 계획](../research/terrain-finetune-plan.md) §1.

| 무엇 | 누가 닫나 |
|---|---|
| 운영진 제출 형식이 hwp 필수인가 pdf 허용인가 | 팀장 확인 |
| 11월 운영진 중간점검이 공식 일정인가 | 팀장 확인 |
| `hub-research` 와 `research` 가 같은 내용을 본다 | 홈페이지 구조화 때 |
| 연구 허브를 성격별로 다시 가른다 (PoC 시험 · 자료 조사 · 파이프라인 점검 · 본 PoC) | 홈페이지 구조화 때 |
| `jay/MAI_UNIVERSE` 와 `jay/인공지능사관학교/MAI_UNIVERSE` 두 벌 | 다음에 논의 |
| 트랙 B 주담당 재지정 (9/30 이후) | 팀 회의 |
