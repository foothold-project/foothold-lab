# 회의록 한 페이지 인쇄

정본은 md다. PDF는 저장소에 올리지 않는다.

기준 격자: `inbox/meang/20260818-rough-terrain-rl-study.md` (험지RL 공부와 접근).
폰트: 나눔고딕 (`fonts-nanum`. `render.sh`가 `print/fonts/`로 복사).

| 파일 | 역할 |
|---|---|
| `oneshot.css` | 공유 격자 · 나눔고딕 |
| `Foothold_260814.html` | 팀·운영 회의 예 |
| `Foothold(박철제님)_260824.html` | 멘토링 예 |
| `render.sh` | 로컬에서 Chrome headless → A4 PDF (git에 넣지 않음) |

새 회차는 `docs/meetings/YYYYMMDD-slug.md` 한 페이지로 적는다. HTML·PDF는 새로 만들지 않는다.
