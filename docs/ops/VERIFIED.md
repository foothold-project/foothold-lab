# 자동화 파이프라인 — 검증 기록

> 2026-08-07~08 · 제로베이스로 팀원과 같은 경로를 실제로 밟아 확인했다.
> "성공 로그"보다 중요한 것은 **여기서 발견한 결함과 교훈**이다.

## 검증 통과 (lab 14회 · rl 5회 실행, 전부 success)

| 경로 | 확인 방법 | 결과 |
|---|---|---|
| 이슈 생성 → 보드 자동 등록 | lab #2 / rl #1 | ✅ |
| start=오늘 / end=+7일 자동 기록 | 보드 화면 + 실행 로그 | ✅ |
| **담당자 자동 지정** (만든 사람) | rl #1 → vfxpedia | ✅ |
| 디스코드 "새 작업" 알림 | 팀장 수신 확인 | ✅ |
| 커밋 `Closes #N` → 이슈 자동 닫힘 | lab #2 (state_reason=completed) | ✅ |
| 닫힘 → 보드 Status=Done | 보드 화면 (내장 워크플로) | ✅ |
| 디스코드 "완료" 알림 | 팀장 수신 확인 | ✅ |
| docs/ push → "문서 갱신" 알림 | 3차 수정 후 로그로 확인 | ✅ |

## 발견한 결함 2건 — 그리고 교훈

### ① rl 워크플로가 등록만 되고 0회 실행 (08-07)
- Public 레포에서 Actions 첫 사용 시 **UI 의 Enable 버튼을 눌러야** 실제로 돈다.
- API 는 누르기 전에도 `enabled: true` 를 돌려줬다 — **API 상태와 실제 동작이 어긋날 수 있다.**
- 해소: 팀장이 Enable 클릭 → 즉시 정상.

### ② docs push 알림이 조용히 skip (08-08) — 세 번 만에 잡음
- 1차 원인: `git diff` — 러너에 저장소가 없어(checkout 안 함) 항상 빈 결과.
- 2차 수정(payload jq 파싱)도 실패 — 그런데 **건너뛰는 분기에 로그가 없어 이유를 볼 수 없었다.**
- 3차: GitHub API 로 커밋 파일 목록을 직접 조회 + **생략 사유를 항상 출력**하게 변경 → 통과.
- ★ 교훈: **조용히 건너뛰는 분기에는 반드시 이유를 출력한다. 침묵은 성공과 구별되지 않는다.**
  (같은 부류 사고가 이번 주에만 4번째 — 스캐너 옥텟, CSS 주입 존재검사, --brand 미반영, 이번 건)

## 시크릿 배치 (실측 확인)

| 위치 | 내용 | 커버 |
|---|---|---|
| org secrets (Public repositories) | PROJECT_TOKEN · DISCORD_WEBHOOK | rl · site |
| lab 레포 secrets | 동일 2종 | lab (Free 플랜은 org 시크릿을 Private 레포가 못 씀) |

⚠️ 토큰 재발급 시 **두 곳 다** 갱신해야 한다.

## 아직 자동화 안 된 것

- end date 초과 알림 / 계획 vs 실제(closedAt) 리포트 — D7
- docs → 웹 포스팅 (컨펌 게이트 포함) — D8
- 주간 리포트는 월요일 09:00 첫 발송 예정 (아직 한 번도 안 돎 — 다음 월요일 확인할 것)

## 9경로 — 이슈 닫힘 → 보드 done date 자동 기입 (08-09 실검증)

- 테스트: 이슈 #7 을 열고 닫아 3회 트리거.
- 1차: "필드 없음 — 생략" 이 **success 로 표시됨** → 로그를 열어보고서야 알았다.
  같은 교훈 재확인: **침묵은 성공과 구별되지 않는다.** → 못 찾으면 실제 필드 목록을 출력하게 수정.
- 2차: 보드에 실제로 `start date`·`end date` 만 있음이 로그로 드러남 (done date 필드 부재).
- 3차: **워크플로가 필드를 직접 생성**(createProjectV2Field)하도록 승급 →
  `done date 필드 생성 완료` → `done date = 2026-08-09 (#7)` **기입 확인.**
- 계획(start/end)은 불변 — 계획 vs 실적 비교가 리뷰의 근거.
- rl 에도 동일 워크플로 배포(보드가 공유라 필드는 이미 존재, 기입만 수행).

## 10경로 — site push → 사이트 갱신 알림 (08-11 실검증)

- site 레포에는 알림 워크플로가 **아예 없었다**(lab·rl 에만 있었음). 신설.
- 1차 실패 **403 Forbidden**. 원인을 "org 시크릿이 낡았다"로 잘못 지목해 팀장에게 갱신을 요청했다.
  진짜 원인은 **파이썬 urllib 의 기본 User-Agent 를 Discord(Cloudflare)가 차단**한 것.
  curl 을 쓰는 lab·rl 은 계속 성공했는데 site 만 실패한 것이 단서였다.
  → **교훈: 같은 일을 하는 기존 성공 사례와 먼저 대조한다. 다른 점이 원인이다.**
- 2차: curl 전환 → **HTTP 204 · "전송 확인"** 로그로 검증 완료.
- 파생 수정: `curl -sS` 는 4xx 를 받아도 종료코드 0 이라 **실패가 초록색으로 보인다.**
  lab·rl 알림에도 같은 맹점이 있어 HTTP 코드 검증을 함께 넣었다. 이제 웹훅이 죽으면 빨간 X.
- 부수 확인: lab 알림은 `docs/` 변경만 발송하는 설계대로 동작(생략 시 이유 출력).

## 11경로 — 웹 팔레트 ↔ 브랜드 토큰 정본 대조 (08-11)

- 빌드 `[1.76] tokencheck` 신설. `foothold-brand/tokens/foothold.tokens.json` 이 색의 단일 진실.
- **팔레트 14종 · 라이트/다크 전부 정본과 일치** 확인(다크 값은 토큰의 `$extensions.foothold.mode.dark`).
- 함정: 토큰이 primitive/semantic 두 계층이라 **이름 마지막 마디로만 찾으면 별칭 문자열을 집는다**
  (`--card ≠ {primitive.color.card}` 오탐 1회). 전체 경로로 짚도록 수정.
- 이제 색이 정본과 어긋나면 **배포가 멈춘다.** 기억이 아니라 기계가 지킨다.
