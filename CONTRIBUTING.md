# 기여 규칙

## 브랜치

문서 레포라 가볍게 간다: **main 에 직접 커밋해도 된다.**
단, 남의 문서를 크게 고칠 때는 PR 로 (리뷰 기회를 주기 위해).

## 커밋 메시지

```
<무엇을 했나 한 줄>  Closes #이슈번호
```

- 이슈 없이 한 자잘한 수정은 `Closes` 생략 가능
- **왜**가 자명하지 않으면 본문에 한 줄 적는다

## 문서 스타일: 세 가지 규칙

1. **추측을 사실처럼 쓰지 않는다.** 미확인은 "미확인"이라고 적는다.
2. **숫자를 인용하면 출처를 밝힌다.** 링크가 있으면 링크, 회의 발언이면 타임코드.
3. **출처에는 등급이 있다**: 공식 문서 > 교재 > 논문 > 해설 블로그 > 백과.
   숫자를 인용할 땐 논문·공식만. (사이트 [용어 사전](https://foothold-project.vercel.app/encyclopedia.html#glossary)의 배지 체계와 동일)

## 회의록

- 타이틀: 팀·운영은 `Foothold_YYMMDD`, 멘토링은 `Foothold(멘토명)_YYMMDD`. 폰트는 나눔고딕
- 회의록 정본은 md 하나다: `docs/meetings/YYYYMMDD-slug.md`
- 인쇄·PDF 는 따로 만들지 않는다. 웹 페이지의 «PDF 저장» 버튼을 쓴다
- 녹음 기반 회의록은 **모든 인용에 타임코드**를 붙인다 (원문 대조 가능하게)
- ⚠️ 금액·개인정보가 들어가는 회의록은 **이 레포(Private)까지만.** 공개 사이트에 내보내지 않는다.
