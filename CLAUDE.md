# CLAUDE.md

**이 저장소의 규칙은 [`AGENTS.md`](AGENTS.md) 하나에 있습니다. 그것을 읽고 따르십시오.**

규칙을 두 곳에 나눠 적으면 반드시 갈라집니다. 그래서 이 파일은 포인터만 둡니다.

---

## 도구마다 읽는 파일이 다릅니다

| 도구 | 자동으로 읽는 파일 |
|---|---|
| Claude Code | `CLAUDE.md` (이 파일) |
| Cursor · Codex | `AGENTS.md` |

**어느 쪽으로 들어와도 같은 규칙에 닿게** 하려고 둘 다 둡니다.
내용은 `AGENTS.md` 에만 씁니다. 이 파일은 고치지 마십시오.

---

## 가장 자주 어기는 것 세 줄 (자세한 것은 AGENTS.md)

1. **작업 전 `git pull origin main`.** 안 하면 남의 작업이 사라집니다.
2. **`docs/` 를 직접 고치지 마십시오.** 팀원 제출은 `inbox/<자기이름>/` 에만.
3. **em dash 를 쓰지 마십시오.** 빌드가 배포를 막습니다.
